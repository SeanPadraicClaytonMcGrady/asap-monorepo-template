# Since .env is gitignored, you can use .env.example to build a new `.env` file when you clone the repo.
# Keep this file up-to-date when you add new variables to \`.env\`.

# This file will be committed to version control, so make sure not to have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env` and populate it with your secrets.

# The database URL is used to connect to your database.
POSTGRES_URL="postgres://postgres:postgres@localhost:5432/dev?sslmode=disable"

# You can generate the secret via 'openssl rand -base64 32' on Unix
# @see https://www.better-auth.com/docs/installation
AUTH_SECRET='supersecret'

# Preconfigured Discord OAuth provider, works out-of-the-box
# @see https://www.better-auth.com/docs/authentication/discord
AUTH_DISCORD_ID='discordAuthId'
AUTH_DISCORD_SECRET='discordAuthSecret'

# Required for AI integration
GEMINI_API_KEY="geminiKey"

# Google OAuth (Required)
AUTH_GOOGLE_ID="googleAuthKey"
AUTH_GOOGLE_SECRET="googleAuthSecret"

# Apple OAuth (Optional - remove if not using yet)
AUTH_APPLE_ID="appleAuthKey"
AUTH_APPLE_SECRET="appleAuthSecret"

# LocationIQGeocoder API Key
GEOCODING_API_KEY="geocodingKey"

BASE_URL="baseUrl"